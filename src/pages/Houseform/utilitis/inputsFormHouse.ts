import {formInput} from '../../../models/forminput'

export const inputM2: formInput = { 
    step: 0.2,
    defaultValue: 8,
    min: 4,
    max: 12,
    precision: 2,
}
export const inputBedroom : formInput= { 
  step: 1,
  defaultValue: 1,
  min: 1,
  max: 5,
  precision: 1,
}